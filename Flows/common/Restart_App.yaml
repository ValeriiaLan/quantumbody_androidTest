appId: ${appId}
---
- clearState
- waitForAnimationToEnd

- runScript:
      script: |
          const { execSync } = require('child_process');
          execSync("adb shell input keyevent 3"); 
          execSync("adb shell monkey -p ${appId} -c android.intent.category.LAUNCHER 1");

- waitForAnimationToEnd
